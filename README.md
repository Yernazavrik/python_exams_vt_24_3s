🧩 1. Фабричный метод — создание разных кастомных кнопок

Задание:
Реализуй фабрику WidgetFactory, которая по названию типа возвращает кастомный PyQt5-виджет кнопки:

"red" → красная кнопка с белым текстом

"flat" → плоская кнопка без рамок

"icon" → кнопка с иконкой

Требования:

Использовать абстрактный класс AbstractButtonFactory с методом create_button().

В интерфейсе отобразить несколько кнопок, созданных через фабрику.

Каждая кнопка должна быть виджетом QPushButton с разными стилями.

🧩 2. Абстрактная фабрика — создание тем оформления

Задание:
Создай две фабрики виджетов: LightThemeFactory и DarkThemeFactory.
Каждая создаёт согласованный набор элементов интерфейса (например, кнопка и метка):

светлая тема: белый фон, чёрный текст

тёмная тема: тёмно-серый фон, белый текст

Требования:

Использовать общий интерфейс WidgetFactory с методами create_button() и create_label().

Приложение должно позволять переключать тему кнопкой “Switch Theme”, которая пересоздаёт виджеты через другую фабрику.

🧩 3. Строитель — создание сложного диалогового окна

Задание:
Реализуй паттерн “Строитель” для создания сложного диалога CustomDialog (например, форма регистрации).
У диалога есть:

заголовок

текстовые поля (имя, e-mail, пароль)

кнопки OK и Cancel

Требования:

Определи DialogBuilder с методами add_title(), add_fields(), add_buttons().

Разные строители (SimpleDialogBuilder, StyledDialogBuilder) создают формы с разным оформлением (например, разная цветовая схема или шрифт).

Директор (DialogDirector) управляет порядком сборки.

🧩 4. Одиночка — менеджер состояния приложения

Задание:
Создай класс AppSettings (Singleton), который хранит настройки интерфейса: тема, язык, масштаб.
Все окна приложения должны получать доступ к единому экземпляру настроек.

Требования:

Реализовать одиночку с ленивой инициализацией.

Сделать окно с двумя вкладками:

на одной меняются настройки (например, выбор темы),

на другой они отображаются в реальном времени.

Убедиться, что изменения отражаются в обоих окнах.

🧩 5. Комбинированное задание — фабрика + строитель

Задание:
Создай систему генерации панелей инструментов (toolbar) через фабрику и строителя:

Фабрика создаёт тип панели (EditToolbar, ViewToolbar, CustomToolbar).

Строитель добавляет элементы (add_button(), add_separator(), add_label()).

После сборки панель добавляется в главное окно.

Требования:

Интерфейс с QMainWindow, где можно выбрать тип панели из списка (ComboBox).

При нажатии кнопки “Создать” фабрика выбирает нужный класс, строитель формирует панель, и она добавляется к окну.

Можно визуально видеть, что панели разные (по набору кнопок и стилю).